# Chlu Reputation Viewer

Usage:

- use a webserver to host this folder
- use an anchor link like `#did` where did is the multihash of a Chlu DID
- view reputation

Can be deployed on IPFS using `index.html` only